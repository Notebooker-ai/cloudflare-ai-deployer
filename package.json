{
  "name": "cloudflare-ai-deployer",
  "version": "1.0.0",
  "description": "Deploy and manage an OpenAI-compatible endpoint backed by Cloudflare Workers AI \u2014 hosted at open.notebooker.ai",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
